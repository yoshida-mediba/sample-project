language: php

php:
  - 5.3
  - 5.6
  - hhvm

install:
 - composer self-update
 - composer install

script:
 - vendor/bin/phpcs --standard=PSR1 --report=emacs .
 - vendor/bin/phpcs --standard=PSR2 --report=emacs .
 - vendor/bin/phpcs --standard=PEAR --report=emacs .
 - vendor/bin/phpunit unit

notifications:
  email:
    - h-yoshida@mediba.jp
